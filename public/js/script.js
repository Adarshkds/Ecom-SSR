'use strict';

const alert = document.querySelector('.success-error-alert');
const btn = document.querySelectorAll('button');

document.addEventListener('DOMContentLoaded', () => {
    setTimeout(() => {
        alert.style.display = 'none';
    }, 3000);
});





